# generated by specpipe.la — regenerated from the spec, do not edit
# IDX_RESERVE : :: Str -> Int -> Int
glyph IDX_RESERVE = la path. la h. (la i. IF(lt(i)(POW2(h)))(la _. SEQ(write_file(path)(int_to_str(add(i)(1))))(i))(la _. error("xmssidx: key exhausted — all 2^h leaf indices used; a new keypair is required, and REUSING one would leak the leaf secret")))(IDX_READ(path))
# IDX_READ : :: Str -> Int
glyph IDX_READ = la path. str_to_int(read_file(path))
# IDX_CAPACITY : :: Int -> Int
glyph IDX_CAPACITY = la h. POW2(h)
# POW2 : :: Int -> Int
glyph POW2 = la h. bshl(1)(h)
# IF : :: Bool -> a -> a -> a
glyph IF = la c. la t. la f. c(t)(f)("!")
# SEQ : :: a -> b -> b
glyph SEQ = la a. la b. b
export IDX_RESERVE IDX_READ IDX_CAPACITY POW2 IF SEQ 
